TG_BOT_API_ID=<telegram-api-id>
TG_BOT_API_HASH=<telegram-api-hash>
TG_BOT_DATA_DIR=./data
# 数据库类型：sqlite 或 postgresql
TG_BOT_DATABASE=sqlite
# 选择 postgresql 时必须填写完整连接串；也可直接用 TG_BOT_DATABASE_URL 覆盖类型默认值
# TG_BOT_DATABASE_URL=postgresql+psycopg://<user>:<password>@<host>:5432/<database>
TG_BOT_ADMIN_CHAT_IDS=
# BotFather 创建的机器人 Token；未配置时通知功能保持禁用
TG_BOT_NOTIFICATION_BOT_TOKEN=
TG_BOT_ADMIN_BOT_TOKEN=
# 管理 Bot 更新接收方式：long_polling 或 webhook
TG_BOT_BOT_TRANSPORT=long_polling
# Webhook 模式必填：外部可访问的完整 HTTPS 地址，以及 1-256 位随机 secret（生产环境建议至少 32 位）
# TG_BOT_BOT_WEBHOOK_URL=https://<admin-host>/api/telegram/admin-bot/webhook
# TG_BOT_BOT_WEBHOOK_SECRET=<openssl-rand-hex-32>
# 通知时间展示时区（服务生命周期通知使用；任务事件仍使用任务自身时区）
TG_BOT_NOTIFICATION_TIMEZONE=Asia/Shanghai
# 开发环境建议关闭服务启动/停止通知，避免热重载产生大量消息
TG_BOT_SERVICE_LIFECYCLE_NOTIFICATIONS_ENABLED=false
TG_BOT_LOG_LEVEL=INFO
TG_BOT_LOG_FILE=tg-bot.log
TG_BOT_LOG_MAX_BYTES=10485760
TG_BOT_LOG_BACKUP_COUNT=5
# 使用 `openssl rand -base64 48` 生成，不得使用示例文本作为实际密钥
TG_BOT_ADMIN_KEY=<openssl-rand-base64-48>
TG_BOT_ADMIN_ORIGIN=https://<admin-host>
TG_BOT_ADMIN_SESSION_DAYS=30
TG_BOT_API_HOST=0.0.0.0
TG_BOT_API_PORT=8000
TG_BOT_TRANSPORT_KEY_ROTATION_HOURS=24
# 仅在管理 API 前有反向代理时显式填写 CIDR，逗号分隔
TG_BOT_TRUSTED_PROXIES=
# 后续能力开关（默认关闭，不影响现有签到任务）
TG_BOT_BOT_ENABLED=false
TG_BOT_CHANNEL_NOTIFICATIONS_ENABLED=false
TG_BOT_GROUP_MONITOR_ENABLED=false
TG_BOT_GROUP_MONITOR_HISTORY_LIMIT=500

# 自定义命令：HTTP 默认不允许出站；白名单为 scheme+host+port 精确 origin
TG_BOT_COMMAND_HTTP_ALLOWED_ORIGINS=[]
# 凭据仅部署端持有，绑定 HTTPS origin；不得将真实密钥提交到仓库
TG_BOT_COMMAND_HTTP_CREDENTIALS={}
# 示例：{"lookup":{"origin":"https://api.example.com","headers":{"Authorization":"Bearer <secret>"}}}
# Python 默认关闭，必须部署独立 Runner，无主进程执行回退
TG_BOT_COMMAND_PYTHON_ENABLED=false
# TG_BOT_COMMAND_PYTHON_RUNNER_URL=https://runner.example.com
# TG_BOT_COMMAND_PYTHON_RUNNER_TOKEN=<openssl-rand-hex-32>
TG_BOT_COMMAND_MAX_WORKERS=8
TG_BOT_COMMAND_PYTHON_WORKERS=2
TG_BOT_COMMAND_QUEUE_LIMIT=100
TG_BOT_COMMAND_QUEUE_SECONDS=60
TG_BOT_COMMAND_RATE_LIMIT=5
TG_BOT_COMMAND_RETENTION_DAYS=30
