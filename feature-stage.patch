diff --git a/README.md b/README.md
--- a/README.md
+++ b/README.md
@@ -269,6 +269,89 @@
 
 管理后台的调度表单会在保存前预览后续 5 次执行时间；预览按任务时区计算且不会改变任务状态。
 
+### 随机窗口内多次执行
+
+`schedule.execution_count` 默认 `1`，必须是 **1–1000 的整数**；固定时间只能为 `1`。
+随机窗口可为每个符合 `frequency` 的执行日期配置多次执行，例如：
+
+```yaml
+schedule:
+  type: random
+  timezone: Asia/Shanghai
+  frequency: daily
+  start: "06:00"
+  end: "12:00"
+  execution_count: 3
+```
+
+同一日期的时间点一次生成、无放回抽样并排序，精度为秒，包含窗口两端。
+所以 `t1 < t2 < t3`，且每个时间都在 `06:00:00–12:00:00` 内。
+例如首个时间为 `07:12:00`，第二个必定晚于 `07:12:00`，不会重复抽到过去的时间。
+相同配置、日期的计划稳定，预览与重启不会重新抽签；已消费的时间点不会再次执行。
+窗口不支持跨午夜，执行次数也不能超过窗口内可用的整秒时间点数。
+夏令时回拨只采用第一次出现的墙钟时间；跳时日若没有足够真实秒数容纳全部次数，跳过该日期，不移到窗口外。
+
+次数表示计划触发次数，不是成功发送次数。沿用现有调度策略：离线、长任务或目标聊天忙碌
+可能跳过已过期的时间点，不补发；任务失败重试和手动运行不增加该日的计划次数。
+修改已发布任务的调度后，需要重新发布才应用新计划。
+完整配置见 [`examples/random-messages.yaml`](examples/random-messages.yaml)。
+
+### 分组消息库与随机消息
+
+消息库提供分组新建、改名、消息增删改、查询和删除。组名去除首尾空格后不可重复，
+每组最多 1000 条，每条必须为非空白文本，最多 4096 个 UTF-16 字符单位；支持多行消息。
+空分组可以保存，但不能用于随机发送。所有接口使用已有管理员会话及写请求 CSRF/Origin 校验：
+
+| 方法与路径 | 请求 / 返回 |
+| --- | --- |
+| `GET /api/message-library/groups` | `{items: [...]}`，只返回分组元数据 |
+| `POST /api/message-library/groups` | `{name, messages}`，201 返回完整分组 |
+| `GET /api/message-library/groups/{id}` | 完整分组，含 `messages` |
+| `PUT /api/message-library/groups/{id}` | `{name, messages, revision}` |
+| `DELETE /api/message-library/groups/{id}` | JSON 请求体 `{revision}` |
+
+元数据字段为 `id`、`name`、`messageCount`、`revision`、`createdAt`、`updatedAt`。
+更新或删除必须携带读取时的版本号；过期版本、重名或仍被引用的分组返回 409，找不到返回 404，
+配置格式错误返回 422。修改冲突不会覆盖其他编辑者的数据。
+
+发送消息步骤有三种配置方式（不能混用来源字段）：
+
+```yaml
+# 1. 固定消息：旧配置继续有效。
+- type: send_message
+  text: "/checkin"
+
+# 2. 手动候选 / 从消息库一次性导入：独立快照。
+- type: send_message
+  message_mode: random
+  random_source: manual
+  messages:
+    - "早上好"
+    - "今天也要加油"
+    - |-
+      这是一条
+      多行消息
+
+# 3. 实时引用分组：先创建分组，再使用 API 返回的实际 UUID。
+- type: send_message
+  message_mode: random
+  random_source: group
+  message_group_id: "00000000-0000-4000-8000-000000000001"
+```
+
+手动列表直接保存在工作流定义中；导入操作仅把当前分组消息追加到该列表，不保存分组关联，
+此后分组更新或删除都不影响快照。实时来源仅保存分组 ID，**每次执行发送步骤前重新读取**
+分组内容，再随机选一条；分组修改无需重新发布任务即可影响后续发送。
+每次抽取独立，消息可以与上一次重复；执行时间去重不等于消息去重。
+候选消息支持已有上游变量模板，保存、发布和试运行会校验来源与变量；执行时还校验最终渲染内容。
+若实时分组丢失、变空或所选模板变量不可用，会明确失败，不会发送空消息或回退到旧缓存。
+
+被任务草稿或最新发布版本实时引用的分组不能删除或清空（包括归档任务）；
+先改用其他来源并重新发布。独立快照不受引用保护影响。
+升级自动增加 `message_groups` 表，使用独立迁移版本 9，保留版本 8 给执行器功能。
+上线前备份数据库，先升级后端再升级前端；旧后端不识别随机消息字段，回退前须把相关任务改回旧格式。
+
+
 可在任务中分别控制失败和成功通知。未配置 `notifications` 时默认仅发送失败通知：
 
 ```yaml
 diff --git a/examples/random-messages.yaml b/examples/random-messages.yaml
new file mode 100644
--- /dev/null
+++ b/examples/random-messages.yaml
@@ -0,0 +1,28 @@
+name: 每日随机问候
+account: default
+target: "@example_bot"
+schedule:
+  type: random
+  timezone: Asia/Shanghai
+  frequency: daily
+  start: "06:00"
+  end: "12:00"
+  execution_count: 3
+retry:
+  max_attempts: 1
+  backoff_seconds: [30, 60, 120]
+notifications:
+  failure: true
+  success: false
+steps:
+  - type: send_message
+    message_mode: random
+    random_source: manual
+    messages:
+      - "早上好"
+      - "今天也要加油"
+      - |-
+        这是一条
+        多行消息
+# 实时分组：先在消息库中创建非空分组，然后用 API 返回的实际 UUID
+# 替换步骤的 random_source 为 group，移除 messages 并配置 message_group_id。
diff --git a/src/tg_botx/features/checkin/coordinator.py b/src/tg_botx/features/checkin/coordinator.py
--- a/src/tg_botx/features/checkin/coordinator.py
+++ b/src/tg_botx/features/checkin/coordinator.py
@@ -17,6 +17,7 @@
 )
 from tg_botx.features.checkin.executor import CheckinExecutor
 from tg_botx.features.checkin.schedule import next_run_for, schedule_from_task
+from tg_botx.features.message_library.service import validate_task_message_groups
 from tg_botx.infrastructure.persistence.db import (
     Account,
     Task,
@@ -190,8 +191,8 @@
                 # The run may finish before its planned wall-clock time (for
                 # example after a clock adjustment or a delayed scheduler
                 # callback).  Exclude the occurrence that was just consumed,
-                # not only the completion timestamp, so a daily task cannot
-                # be scheduled again later on the same day.
+                # not only the completion timestamp. Multi-run schedules may
+                # still have later occurrences on the same eligible day.
                 values["next_run_at"] = next_run_for(
                     schedule_from_task(current),
                     now=finished,
@@ -290,6 +291,7 @@
                         on_attempt=on_attempt,
                         on_step_status=on_step_status,
                         on_step_response=on_step_response,
+                        message_group_loader=self.database.messages.get_messages,
                     ),
                     task,
                 )
@@ -447,6 +449,7 @@
             raise TaskNotFound("任务不存在")
         if task.archived:
             raise TaskStateError("归档任务不能测试")
+        validate_task_message_groups(self.database, definition)
         execution_definition = definition.model_dump(mode="json")
         execution_task, account = self._execution_snapshot(
             task, execution_definition, task_name=definition.name
 diff --git a/src/tg_botx/features/checkin/executor.py b/src/tg_botx/features/checkin/executor.py
--- a/src/tg_botx/features/checkin/executor.py
+++ b/src/tg_botx/features/checkin/executor.py
@@ -63,10 +63,12 @@
         on_step_status: Callable[..., Awaitable[None]] | None = None,
         on_step_response: Callable[..., Awaitable[None]] | None = None,
         http_client: httpx.AsyncClient | None = None,
+        message_group_loader: Callable[[str], list[str]] | None = None,
     ):
         self._http_client = http_client
         self._owns_http_client = http_client is None
         self.client = client
+        self.message_group_loader = message_group_loader
         self.messages = TelegramMessageAdapter(client)
         self.is_cancelled = is_cancelled or (lambda: False)
         self.on_attempt = on_attempt
 diff --git a/src/tg_botx/features/checkin/schedule.py b/src/tg_botx/features/checkin/schedule.py
--- a/src/tg_botx/features/checkin/schedule.py
+++ b/src/tg_botx/features/checkin/schedule.py
@@ -80,12 +80,14 @@
         or lower.astimezone(zone).utcoffset() != upper.astimezone(zone).utcoffset()
     ):
         # On DST transition dates only sample real seconds inside the wall-clock
-        # window. Never shift a nonexistent time outside the configured window.
+        # window; omit the repeated fold so local times are also strictly ordered.
+        # Never shift a nonexistent time outside the configured window.
         offsets = [
             offset
             for offset in offsets
             if (local := (lower + timedelta(seconds=offset)).astimezone(zone)).date() == day
             and start_clock <= local.time() <= end_clock
+            and local.fold == 0
         ]
     if len(offsets) < schedule.execution_count:
         return []
diff --git a/src/tg_botx/features/checkin/step_models.py b/src/tg_botx/features/checkin/step_models.py
--- a/src/tg_botx/features/checkin/step_models.py
+++ b/src/tg_botx/features/checkin/step_models.py
@@ -32,6 +32,7 @@
         if self.message_mode == "fixed":
             if self.text is None:
                 raise ValueError("固定消息必须配置 text")
+            validate_message_text(self.text)
             if (
                 self.messages is not None
                 or self.message_group_id is not None
 diff --git a/src/tg_botx/features/checkin/steps/send_message.py b/src/tg_botx/features/checkin/steps/send_message.py
--- a/src/tg_botx/features/checkin/steps/send_message.py
+++ b/src/tg_botx/features/checkin/steps/send_message.py
@@ -1,12 +1,19 @@
 from __future__ import annotations
 
+import asyncio
+import secrets
 from collections.abc import Callable
 from typing import TYPE_CHECKING, Any
 
 from tg_botx.features.checkin.condition import (
     render_template,
 )
-from tg_botx.features.checkin.execution_types import ExecutionContext, StepReport
+from tg_botx.features.checkin.execution_types import CheckinError, ExecutionContext, StepReport
+from tg_botx.features.message_library.models import (
+    MessageGroupConflict,
+    MessageGroupNotFound,
+    validate_message_text,
+)
 
 if TYPE_CHECKING:
     from tg_botx.features.checkin.executor import CheckinExecutor
@@ -22,7 +29,28 @@
     step_duration_ms: Callable[[], int],
     report: StepReport,
 ) -> None:
-    text = render_template(str(step["text"]), context.variables)
+    if step.get("message_mode", "fixed") == "random":
+        if step.get("random_source", "manual") == "group":
+            if self.message_group_loader is None:
+                raise CheckinError("当前执行器未配置消息库，无法读取实时分组")
+            try:
+                messages = await asyncio.to_thread(
+                    self.message_group_loader, step["message_group_id"]
+                )
+            except (MessageGroupNotFound, MessageGroupConflict) as exc:
+                raise CheckinError(str(exc)) from exc
+        else:
+            messages = step.get("messages", [])
+        if not messages:
+            raise CheckinError("随机消息候选列表为空")
+        raw_text = secrets.choice(messages)
+    else:
+        raw_text = str(step["text"])
+    text = render_template(raw_text, context.variables)
+    try:
+        validate_message_text(text)
+    except ValueError as exc:
+        raise CheckinError(str(exc)) from exc
     context.current_message = await self.client.send_message(context.entity, text)
     context.baseline = max(context.baseline, context.current_message.id)
     context.editable_message_ids.clear()
diff --git a/src/tg_botx/features/checkin/tasks.py b/src/tg_botx/features/checkin/tasks.py
--- a/src/tg_botx/features/checkin/tasks.py
+++ b/src/tg_botx/features/checkin/tasks.py
@@ -11,6 +11,7 @@
     TaskStateError,
 )
 from tg_botx.features.checkin.schedule import next_run_for, schedule_from_task
+from tg_botx.features.message_library.service import validate_task_message_groups
 from tg_botx.infrastructure.persistence.db import (
     Account,
     Task,
@@ -62,6 +63,7 @@
         account = self.database.get_account(definition.account)
         if account is None:
             raise AccountNotFoundError("任务绑定的账号不存在")
+        validate_task_message_groups(self.database, definition)
         task = self.database.save_task(self._task_from_definition(account, definition))
         self._publish_task_updated(task.id)
         return task
@@ -78,6 +80,7 @@
         account = self.database.get_account(definition.account)
         if account is None:
             raise AccountNotFoundError("任务绑定的账号不存在")
+        validate_task_message_groups(self.database, definition)
 
         schedule = definition.schedule
         if schedule.start_date is None:
 diff --git a/src/tg_botx/features/checkin/validation.py b/src/tg_botx/features/checkin/validation.py
--- a/src/tg_botx/features/checkin/validation.py
+++ b/src/tg_botx/features/checkin/validation.py
@@ -410,9 +410,7 @@
                 _validate_template(step["text"], f"{step_path}.text", possible)
             elif step.get("random_source", "manual") == "manual":
                 for message_index, message in enumerate(step["messages"]):
-                    _validate_template(
-                        message, f"{step_path}.messages[{message_index}]", possible
-                    )
+                    _validate_template(message, f"{step_path}.messages[{message_index}]", possible)
         elif kind == "wait_message":
             timeout = step.get("timeout_seconds", 60)
             if not isinstance(timeout, int) or isinstance(timeout, bool) or timeout < 1:
 diff --git a/src/tg_botx/features/checkin/workflows.py b/src/tg_botx/features/checkin/workflows.py
--- a/src/tg_botx/features/checkin/workflows.py
+++ b/src/tg_botx/features/checkin/workflows.py
@@ -7,6 +7,7 @@
     TaskStateError,
 )
 from tg_botx.features.checkin.schedule import next_run_for
+from tg_botx.features.message_library.service import validate_task_message_groups
 from tg_botx.infrastructure.persistence.db import (
     WorkflowVersion,
     utc_now,
@@ -45,6 +46,7 @@
             definition = TaskDefinition.model_validate(task.config)
         except Exception as exc:
             raise TaskStateError("当前任务配置无效，无法发布") from exc
+        validate_task_message_groups(self.database, definition)
         schedule = definition.schedule
         if task.enabled:
             try:
 diff --git a/src/tg_botx/features/message_library/models.py b/src/tg_botx/features/message_library/models.py
--- a/src/tg_botx/features/message_library/models.py
+++ b/src/tg_botx/features/message_library/models.py
@@ -53,13 +53,13 @@
 
 
 class MessageGroupUpdate(MessageGroupWrite):
-    revision: int = Field(ge=1, strict=True)
+    revision: int = Field(ge=1, le=2_147_483_646, strict=True)
 
 
 class MessageGroupDelete(BaseModel):
     model_config = ConfigDict(extra="forbid", strict=True)
 
-    revision: int = Field(ge=1, strict=True)
+    revision: int = Field(ge=1, le=2_147_483_646, strict=True)
 
 
 class MessageGroupSummary(BaseModel):
diff --git a/src/tg_botx/interfaces/admin/routes/tasks.py b/src/tg_botx/interfaces/admin/routes/tasks.py
--- a/src/tg_botx/interfaces/admin/routes/tasks.py
+++ b/src/tg_botx/interfaces/admin/routes/tasks.py
@@ -11,6 +11,7 @@
     CheckinService,
 )
 from tg_botx.features.checkin.schedule import next_runs
+from tg_botx.features.message_library.service import validate_task_message_groups
 from tg_botx.infrastructure.persistence.db import (
     Database,
     utc_now,
@@ -63,6 +64,7 @@
                 422,
                 details=[{"path": "definition.account", "message": "Telegram 账号不存在"}],
             )
+        validate_task_message_groups(database, body.definition)
         return {
             "valid": True,
             "definition": body.definition.to_api_dict(),
