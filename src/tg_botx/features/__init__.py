"""Optional Telegram capabilities built on top of the check-in runtime.

Submodules are intentionally not imported eagerly.  This keeps compatibility
imports cycle-free and allows lightweight consumers to load one feature at a
time.
"""
